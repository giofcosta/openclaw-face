# OpenClaw Face Server Bridge Configuration
# Copy this to .env.local and fill in your values

# Bridge server URL (the openclaw-face-server)
VITE_BRIDGE_URL=http://localhost:18796

# API key for authentication (must match the server's API_KEY)
VITE_BRIDGE_API_KEY=your-api-key-here
